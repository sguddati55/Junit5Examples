node {

stage('clone workspace') {
git 'https://github.com/sguddati55/Junit5Examples.git'
echo "pulled the code"
}
 
//def props = readProperties  file:'envvariables.properties'
//def Var1= props['slackTo']
//echo "Var1=${Var1}"

        stage('Example') {
            
                echo "Email passed from Properties File - ${env.emailTo}"
                echo "Email with params - ${params.emailTo}"
                echo "Slack Id to be Notified - ${env.slackTo}"
                echo "Test suite Name from Properties File - ${env.testuitename}" 
                echo "Enviornment Name from Build Paramaeters - ${env.SUT_NAME}" 
                echo "Test cloud Name from Properties Content - ${env.CLOUD_NAME}" 
                script {currentBuild.description = "${params.SUT_NAME}" }
            
        }
    

}
